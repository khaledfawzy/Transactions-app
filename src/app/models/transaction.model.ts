export interface Transaction {
  emp_code: string;  // Ensure this matches the API response
  punch_time: string;
  upload_time: string;
  first_name: string;
  nickname: string;
  photo: string;
  // Add other properties based on your API response
}
